services:
  db:
    image: postgres:16-alpine
    container_name: we_learn_greek_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: we_learn_greek
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d we_learn_greek"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
